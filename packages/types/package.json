{
  "name": "types",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest --run",
    "test:types": "tsd",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.js",
    "lint:fix": "eslint . --ext .ts,.js --fix"
  },
  "devDependencies": {
    "ts-expect": "^1.3.0",
    "vitest": "^3.2.3"
  },
  "dependencies": {
    "zod": "^3.25.64"
  }
}
